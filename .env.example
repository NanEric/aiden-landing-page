# 下载链接配置
NEXT_PUBLIC_DOWNLOAD_URL_MAC="https://example.com/download/mac"
NEXT_PUBLIC_DOWNLOAD_URL_WINDOWS="https://example.com/download/windows"

# 版本信息
NEXT_PUBLIC_APP_VERSION="v1.0.2"

# Vercel KV Configuration (在 Vercel 控制台中配置，无需添加到 .env 文件)
# KV_REST_API_URL 和 KV_REST_API_TOKEN 会在 Vercel 部署时自动配置
# 本地开发时需要在 Vercel 控制台获取这些值并添加到 .env.local
